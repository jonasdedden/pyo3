`experimental-inspect`: type annotations written by hand, as in `#[pyo3(signature = (x: "datetime.date"))]`, are now emitted as real expressions with the imports they need, instead of as string annotations naming something the stub never imports.
