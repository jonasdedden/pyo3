`experimental-inspect`: `__next__` and `__anext__` written as `-> Option<T>` (or `-> PyResult<Option<T>>`) are now introspected as `-> T`. `None` is how PyO3 spells `StopIteration`/`StopAsyncIteration`, not a value the iterator yields.
